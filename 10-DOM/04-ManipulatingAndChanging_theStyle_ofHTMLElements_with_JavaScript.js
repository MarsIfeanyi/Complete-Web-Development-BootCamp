/**
 * Hint:JS use camelCase for naming thus apply camelCase while using the css or style selectors. Eg fontSize and not fontsize... style names in css is same in JS but there are no dashes and it is camelCased. Also in JS you specify the values as a string.
 */

document.querySelector("h1").style.fontSize = "10rem";

document.querySelector("button").style.backgroundColor = "yellow";
