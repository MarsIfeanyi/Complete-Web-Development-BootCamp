/**
 * WAYS OF ADDING JAVASCRIPT TO OUR WEB PAGE
 *
 * 1. Inline
 * 2. Internal
 * 3. External
 *
 * We can add inline JS by simply adding an attribute into the HTML tag.
 *
 * Hint: Inline JS has a lot downside of inline css. its not modular and it is difficult to debug and thus its not a good practice, try to avoid it if you can
 */
